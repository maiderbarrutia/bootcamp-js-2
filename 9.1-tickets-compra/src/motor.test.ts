import { calcularPrecioTotal } from "./motor";

describe("calcularPrecioTotal", () => {
  it("Lanza una excepción si el NIF no tiene 8 cifras", () => {
    // Arrange
    //   const numero = 1234567;
    //   const letra = "Z";
    // Assert
    //   expect(() => calcularPrecioTotal(numero, letra)).toThrow();
  });
});
